---
name: commit
description: יצירת commit לשינויים הנוכחיים ב-StudyTop ודחיפתו ל-remote — בודק git status/diff/log, מרכיב הודעת קומיט באנגלית לפי הסטנדרט של הריפו, ומבצע commit + push ישירות בלי לעצור לאישור. הפעילו עם /commit.
---

# /commit — יצירת commit ודחיפה

בדקו את מצב הריפו, הרכיבו הודעת commit ברורה באנגלית, צרו commit ודחפו אותו ל-remote — **ישר, בלי לעצור ולבקש אישור**. המשתמשת ביקשה במפורש שהסקיל הזה לא ישאל לפני commit/push; דווחו על מה שנעשה אחרי העובדה, לא לפני.

**שימוש:** `/commit`

## הוראות

### 1. בדיקת מצב הריפו

הריצו במקביל:

```bash
git status
git diff
git diff --staged
git log --oneline -10
git status -sb
```

בדקו גם אם הענף עוקב אחרי remote ואם יש הבדל בין המקומי לרחוק (`ahead`/`behind` בפלט של `git status -sb`).

אם אין שום שינוי (לא ב-staged, לא ב-unstaged, בלי untracked רלוונטי) — ספרו למשתמשת שאין מה לקמט ועצרו.

### 2. סינון לפני staging

עברו על פלט `git status`. **לפני** הוספת קבצים, בלי לעצור ולשאול — קבעו לפי שיקול דעת ודווחו על ההחלטה בסוף:

- קבצים שנראים כמו סוד/קרדנשלס (`.env`, `*credentials*`, מפתחות API) — **אף פעם לא** להוסיף אותם, ללא יוצא מן הכלל. ציינו בדו"ח הסופי שהם הוצאו.
- קבצים שנראים כמו scratch/זמניים מקומיים ולא קשורים לשינוי הנוכחי (שמות עם `tmp`/`scratch`, נתיבים הרדקודדים למחשב המקומי, קבצים שלא נראים כמו חלק מהפיצ'ר/תיקון) — השאירו מחוץ לקומיט כברירת מחדל וציינו זאת בדו"ח.
- שאר הקבצים ששונו/נוצרו כחלק מהעבודה הנוכחית — כללו בקומיט.
- אל תשתמשו ב-`git add -A` או `git add .`. הוסיפו קבצים ספציפיים בשמם, כדי לא לגרור בטעות קבצים לא-קשורים.

### 3. הרכבת הודעת הקומיט

הודעות הקומיט בריפו הזה **באנגלית**, שורת סיכום קצרה בלשון ציווי (למשל `Add`, `Fix`, `Update`, לא `Added`/`Fixed`), ולפעמים גוף עם פירוט. הביטו ב-`git log` שהרצתם כדי להתאים לסגנון הקיים.

- אם השינויים קשורים בבירור ל-issue ספציפי (מהשיחה, מהענף, או מהקוד עצמו) — ציינו את זה בגוף ההודעה (`Refs #<number>`). אם לא ברור, פשוט דלגו על זה — אל תמציאו קשר.
- אם יש כמה שינויים שברור שהם לא-קשורים לגמרי (לדוגמה תיקון UI ולצידו refactor בתחום אחר) — פצלו לכמה commits נפרדים לפי שיקול דעת, בלי לעצור ולשאול.
- ההודעה מסכמת **למה** השינוי נעשה, לא רק **מה** השתנה — הקוד עצמו כבר מראה מה השתנה.

### 4. ביצוע ה-commit

```bash
git add <קבצים ספציפיים>
git commit -m "$(cat <<'EOF'
<כותרת קצרה בלשון ציווי>

<גוף אופציונלי, אם נדרש הסבר נוסף>

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
git status
```

### 5. דחיפה ל-remote

```bash
git push
```

- אם הענף לא עוקב אחרי remote (push ראשון), השתמשו ב-`git push -u origin <branch>`.
- אם ה-push נדחה כי הענף המרוחק התקדם (non-fast-forward) — **אל תעשו force push**. עצרו, ספרו למשתמשת מה קרה, והציעו `git pull --rebase` או בדיקה ידנית שלה לפני שממשיכים.
- ודאו אחרי ה-push עם `git status` שהענף המקומי מסונכרן עם המרוחק.

### 6. דיווח

אחרי שהכל בוצע (לא לפני), דווחו בקצרה למשתמשת: אילו קבצים נכנסו לקומיט, אילו הוצאו במכוון (ולמה), הודעת הקומיט, ושה-push הצליח (או מה קרה אם לא).

### 7. גבולות

- **בלי force push** — לעולם לא `--force`/`--force-with-lease` בלי בקשה מפורשת של המשתמשת לפעולה הזו הספציפית.
- **בלי `--no-verify` / `--no-gpg-sign`** — אם pre-commit hook נכשל, תקנו את הבעיה האמיתית והריצו commit חדש; אל תעקפו את ה-hook.
- **בלי `--amend`** — תמיד commit חדש, אלא אם המשתמשת מבקשת amend במפורש.
- אם `git push` דורש אימות/הרשאות שלא קיימות בסביבה, דווחו למשתמשת מה חסר ועצרו — אל תנסו לעקוף.
