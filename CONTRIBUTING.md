# תרומה ל-StudyTop

נקודת הכניסה היחידה לפיתוח ב-StudyTop. קראו את הקובץ הזה לפני כל עבודה על הריפו — הוא כל מה שצריך לדעת על הפרויקט כדי לתכנן ולממש בו שינויים.

## מה זה StudyTop

דשבורד לימודי אישי בעברית (RTL) לתלמידות/ים ז'-י"ב: מערכת שעות, שיעורי בית, מתכנן שבועי חכם, מחולל בחנים מותאם-כיתה, מחשבון ציונים, ממיר יחידות, ובודק איות/מסכם טקסטים בגרסת דמו מקומית. בהשראת הרעיון הכללי של WebTop הבית-ספרית, בלי עיצוב/לוגו/קוד/מיתוג שלה ובלי חיבור אליה.

לפירוט מלא של כל מסך ומה עובד היום — ראו [README.md](README.md).

## Stack

**HTML/CSS/JavaScript רגיל — בלי npm, בלי שרת build, בלי TypeScript.** טיפוסים מתועדים ב-JSDoc בתוך `js/models/types.js`, לא בקבצי `.ts`. אין תלות בשום ספרייה או API חיצוני. כל הנתונים נשמרים ב-`localStorage` בדפדפן בלבד, בנפרד לכל משתמש (אין שרת, אין ענן, אין אימות מול WebTop).

**חשוב:** אי אפשר לפתוח את `index.html` בלחיצה כפולה — מודולי JS (`type="module"`) חסומים מ-`file://`. יש להריץ שרת סטטי מקומי:

```bash
python -m http.server 8080
```

או תוסף Live Server ב-VS Code (ראו README).

## מבנה הפרויקט

```
css/             עיצוב (RTL, בהיר/כהה, רכיבים משותפים, מסך התחברות/onboarding)
js/
  app.js         נקודת כניסה — בודקת התחברות/סטטוס פרופיל, מציגה אימות/onboarding/דשבורד
  router.js      ניתוב בין מסכים לפי hash בכתובת
  models/        טיפוסים (JSDoc) — types.js
  data/          שכבת localStorage (פר-משתמש) — storage.js
  auth/          הרשמה/התחברות: הצפנת סיסמה, session, טפסים
  onboarding/    מסך הגדרת פרופיל לימודי (חד-פעמי) + קטלוג מקצועות/כיתות
  utils/         לוגיקת חישוב (המרות, ציונים, מתכנן שיעורי בית, סיכום, איות)
  components/    רכיבי UI משותפים (סיידבר, מודל, טוסט, מצב ריק)
  quiz/          קטלוג נושאים ללימוד למחולל הבחנים
  rating/        חלון דירוג האפליקציה
  pages/         מסך לכל פריט בתפריט (מוגנים — נגישים רק אחרי התחברות+פרופיל)
tests/           דף בדיקות עצמאי לפונקציות החישוב (בלי npm — נפתח ישירות בדפדפן)
```

## סטנדרטים לקוד

- **קבצים קטנים, מופרדים לפי תפקיד** — לא קובץ ענק אחד. פיצ'ר חדש בדרך כלל אומר: פונקציה ב-`utils/`, מסך ב-`pages/`, ואולי רכיב משותף ב-`components/` אם נדרש.
- **RTL תמיד** — כל מסך/רכיב חדש חייב להיראות טוב ב-RTL. שורת דירוג הכוכבים היא היוצא מן הכלל המכוון (תמיד LTR).
- **בלי תלויות חיצוניות** — לא להוסיף npm packages, CDN scripts, או קריאות API חיצוניות. אם פיצ'ר "אמיתי" (כמו סיכום מבוסס-AI) לא בר-מימוש בלי שרת/API key — בונים גרסת דמו/mock מקומית, לא מדלגים על הפיצ'ר.
- **הפרדה בין נתוני משתמש** — כל קריאה/כתיבה ל-localStorage חייבת לעבור דרך `js/data/storage.js` ולהיות מסוננת למשתמש המחובר. אסור לערבב נתונים בין משתמשים.
- **טון תומך, לא שיפוטי** — טקסטים למשתמשת (אזהרות, הודעות ריקות, הצעות תזמון, "כמה חסר ליעד") תמיד בניסוח מעודד, לא מתחרה/משווה.
- **בלי נתוני דמה מזויפים בברירת מחדל** — משתמש חדש מתחיל עם המקצועות שבחר בפועל ב-onboarding, לא עם דוגמאות מזויפות.
- **הערות קוד חדשות באנגלית** — קוד חדש או קוד קיים שנערך בכל זאת מקבל הערות באנגלית. הערות עבריות קיימות נשארות כפי שהן ולא מתורגמות רטרואקטיבית. טקסטים למשתמשת (UI) נשארים בעברית תמיד — זה נוגע רק להערות קוד.

## בדיקות

`tests/` מכיל דף HTML עצמאי שבודק את פונקציות החישוב (ציונים, המרות, מתכנן שיעורי בית וכו') בלי npm — פותחים אותו ישירות בדפדפן (או דרך אותו שרת סטטי) ובודקים שכל הבדיקות ירוקות. כל פונקציית חישוב חדשה או משמעותית שמשתנה צריכה בדיקה מתאימה שם.

## זרימת עבודה עם Issues

הריפו עובד עם ארבעה סקילים ב-`.claude/skills/` שמנהלים את המחזור **issue → plan → implement → verify**:

| שלב | סקיל | מה הוא עושה |
|---|---|---|
| 1. פתיחת issue | `/new-issue` | ראיון קצר על הרעיון/הבאג, בדיקת הקוד הקיים, יצירת issue מובנה ב-GitHub (**באנגלית**) עם תיוג עדיפות |
| 2. תכנון | `/plan-issue <מספר>` | תכנון מעמיק איטרטיבי (כמה סבבי ביקורת) עד שהתוכנית שלמה, ופרסום שלה כתגובה ל-issue |
| 3. מימוש | `/implement-issue <מספר>` | ביצוע התוכנית שאושרה, ופרסום דו"ח מימוש כתגובה ל-issue |
| 4. בדיקת מימוש | `/verify-issue <מספר>` | השוואת הקוד בפועל לתוכנית ולקריטריוני הקבלה, ופרסום דו"ח בדיקה כתגובה ל-issue |

**תיוג עדיפות** (בשימוש ב-`/new-issue`):
- **Blocking** — משהו שבור/חוסם, לתקן עכשיו
- **VNH** (Very Nice to Have) — שיפור משמעותי, בקרוב
- **Dream** — רעיון נחמד, לא דחוף

**תיוג התקדמות** (בשימוש ב-`/plan-issue`, `/implement-issue` ו-`/verify-issue`): `Planning` → `Planned` → `Implementing` → `Implemented` → `Verifying` → `Verified` (או `Needs Fixes` אם נמצאו פערים).

זרימת העבודה משתמשת בפקודת `gh` (GitHub CLI) — יש להיות מחוברים דרך `gh auth login`.

**קומיט:** הסקילים למעלה לא מבצעים commit בעצמם. לקמט שינויים ולדחוף אותם מריצים `/commit` בנפרד — בודק את מצב הריפו, מרכיב הודעה באנגלית לפי הסגנון הקיים, ומבצע commit + push אחרי אישור.
